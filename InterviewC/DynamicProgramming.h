#include <vector>

bool can_win_print_solution(const std::vector<int> &a, int s, const std::vector<int> &m = {}, bool t = true);
bool can_win(const std::vector<int> &a, int s, bool t = true);
bool will_not_lose(const std::vector<int> &a, int s);